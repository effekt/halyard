node_modules/
dist/
.next/
.turbo/
*.tsbuildinfo
.DS_Store

.env
.env.local
.env.*.local

coverage/
.halyard/

# Generated on demand by `pnpm map` — a committed codebase map goes stale and
# conflicts on every branch that adds a unit.
.repomix/

# jscpd report output
report/

# Vendor scrub list — kept private; see vendor-terms.example.txt
scripts/vendor-terms.txt

# Skills installed by the skills CLI into the project. Third-party content that is not
# ours to redistribute, and it arrives as symlinks into .agents/ which git would follow.
# The decision skill is ours and stays tracked.
/.agents/
skills-lock.json
/.claude/skills/*
!/.claude/skills/decision/
